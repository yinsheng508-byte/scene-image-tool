# 当前任务列表

任务来源：`docs/ai-project-template-项目结构迁移落地规划.md`；macOS 后续主线见 `docs/current/macos-main-app-development-requirements.md` 和 `docs/current/macos-main-app-task-cards.md`。

| 阶段 | 状态 | 可演示路径 | 修改范围 | 验收点 |
|---|---|---|---|---|
| Phase 0：冻结和保护现场 | 已完成 | 查看分支和 `.migration-backups/` | Git 分支、本地快照 | 已创建迁移分支和迁移前快照 |
| Phase 1：安装 AI 协作治理脚手架 | 已完成 | 读取 `AGENTS.md` 和 `docs/INDEX.md` | 根入口、`docs/current`、`docs/architecture` | 当前事实有唯一入口，24 个治理文件已创建 |
| Phase 2：建立忽略规则和产物策略 | 已完成 | `git status --ignored` | `.gitignore` | `.migration-backups/`、备份目录、ZIP、node_modules 等已被忽略 |
| Phase 3：从索引移除明确构建产物 | 已完成 | `git ls-files desktop/dist2 desktop/_tmp 'desktop/test_*.png'` | Git 索引 | 目标路径已从索引清空，本地 `dist2` / `_tmp` 保留 |
| Phase 4：迁移代码目录到 `code/desktop/` | 已完成 | `npm --prefix code/desktop run font:probe` | `desktop/` -> `code/desktop/` | 旧目录不存在，索引无 `desktop` 跟踪项，字体探针通过 |
| Phase 5：重排历史文档 | 已完成 | `docs/archive/README.md` | 根目录文档、`docs/archive` | `docs/` 根只保留当前导航、长期背景和迁移规划；历史资料分三类归档 |
| Phase 6：治理大资源 | 已完成 | `docs/architecture/resources.md` | fonts、vendor、fixtures、敏感文件策略 | 依赖型大资源保留并登记策略；敏感/样例/图片移出索引并进入 `local-artifacts/` |
| Phase 7：根目录清理 | 已完成 | `docs/current/root-structure.md` | 临时碎片、最终上下文 | Git 跟踪的根入口只保留治理入口、`code/`、`docs/`；本地碎片进入 `local-artifacts/` |
| Phase 8：验证、收口和验收 | 已完成 | `docs/current/acceptance.md` | 测试、验收文档 | 自动化验收通过，人工页签冒烟建议已记录 |
| 迁移后规范检查 | 已完成 | `docs/current/session-log.md` | 活动文档、测试脚本 | 已修正过期事实和 noisy smoke 脚本，自动检查通过 |
| 迁移后本地构建垃圾清理 | 已完成 | `docs/current/root-structure.md` | 本地备份、构建产物、无引用探针 | 已物理删除约 21.6GB 忽略文件和旧产物；已从源码删除 3 个无引用历史探针/图片；保留运行必需的 `fonts/`、`vendor/`、`node_modules/` |
| GitHub 公开同步执行 | 已上线，已补齐 | `https://github.com/yinsheng508-byte/scene-image-tool` | 干净导出仓库、公开远端、Mac clone 验收、覆盖校验 | public 仓库已创建；`main` 和 `platform/macos-bootstrap` 保持同步；代码 hash 对比缺失 0、内容不一致 0；已补齐 40 份中文归档文档；public 源码文件数 157；不含字体二进制、Windows LibreOffice runtime、VC redist exe 和 `.bootstrap` |
| GitHub 同步与 macOS 准备 | 已上线，进入 macOS 开发前置 | `docs/current/github-sync-macos-plan.md` | GitHub 远端、历史大文件、资源外部化、macOS adapter | 已完成公开干净导出仓库上线；后续从 `platform/macos-bootstrap` 开始 macOS adapter 和打包配置开发 |
| Windows / macOS 并行开发方案 | 已完成 | `docs/current/win-mac-parallel-development.md` | 分支模型、平台 adapter、CI、GitHub Project、发布节奏 | 已写明共享核心 + 平台适配层 + 平台构建配置路线，供 GitHub 同步后两端并行开发 |
| macOS 开发落地指令 | 已完成 | `docs/current/mac-development-runbook.md` | Mac 环境、clone、启动、smoke、首批任务 | 已给出 Xcode CLT、Homebrew、Git LFS、Node 22、LibreOffice、Electron 开发启动和每日开发命令 |
| macOS 首次开发启动验证 | 已完成 | `~/dev/scene-image-tool` | Mac 环境、GitHub clone、`code/desktop/` 依赖安装、Electron 开发启动、拼图 smoke | 2026-08-28 已在 macOS clone `platform/macos-bootstrap`；Node 22/npm/git/git-lfs/LibreOffice 可用；`npm --prefix code/desktop ci` 通过；`npm exec electron .` 能打开 Electron 主界面；`puzzle:shadow:smoke` 和 `puzzle:text:smoke` 通过；未运行第一阶段禁跑命令 |
| macOS LibreOffice runtime 探测 | 已完成，PR #1 已合并 | `code/desktop/platform/darwin/libreoffice-runtime.js` | Darwin platform adapter、`main.js` runtime resolve、Node 兜底预检 | PR #1 已合并到 `platform/macos-bootstrap`；能识别 `/Applications/LibreOffice.app/Contents/MacOS/soffice`、`/opt/homebrew/bin/soffice`、`/usr/local/bin/soffice` 和 `LIBREOFFICE_PATH`；返回 `ok/platform/capability/source/path/version/warnings/errorCode/message/actions`；缺失和 embedded 模式不崩溃；Electron 启动自检已命中 `system_app` |
| macOS 主应用开发改造规划 | 已完成 | `docs/current/macos-main-app-development-requirements.md`、`docs/current/macos-main-app-task-cards.md` | Mac 主应用需求、platform adapter 架构、导出链路、打包、CI、资源治理、任务拆分 | 已拆出 M0-M11 阶段路线、Gate A-D 验收门禁、MAC-00 至 MAC-13 任务卡；当前已推进到 MAC-09，PR #11 合并后进入 MAC-10 |
| 代码全面审查与文档校准 | 已完成 | `docs/current/macos-main-app-development-requirements.md`、`docs/current/macos-main-app-task-cards.md` | Mac 平台边界、Office COM 预检、LibreOffice 文案、打包资源、CI、IPC/cancel hardening | 已确认并写入文档：macOS Office engine 当前会进入 PowerShell 风险、LibreOffice UI 文案偏 Windows、`extraResources` 仍引用缺失 Windows runtime、`check:lo-runtime` 是 Windows-only、shell IPC 和网络取消需独立 hardening；基础校验通过 |
| MAC-01：platform adapter 总壳 | 已完成，PR #3 已合并 | `code/desktop/platform/index.js`、`code/desktop/platform/common/capability-result.js` | 平台总入口、capability normalizer、`main.js` Darwin runtime 入口 | `main.js` 已通过 `currentPlatformAdapter.runtime.resolveLibreOffice()` 获取 macOS runtime；Windows runtime resolve 保持 legacy passthrough；node check、adapter 直接调用和 puzzle smoke 通过 |
| MAC-02：跨平台进程终止 adapter | 已完成，PR #4 已合并 | `code/desktop/platform/common/process-utils.js`、`code/desktop/platform/darwin/process-tree.js`、`code/desktop/platform/win32/process-tree.js` | 导出取消进程终止边界、`main.js` adapter 调用 | macOS 取消不调用 `taskkill`；Windows adapter 保留 `taskkill /T /F`；非 Windows `getProcessNameByPid` 不进入 PowerShell；node check、adapter 直接调用和 puzzle smoke 通过 |
| MAC-03：统一导出 capability / health 返回结构 | 已完成，PR #5 已合并 | `code/desktop/platform/common/health-report.js`、`code/desktop/main.js`、`code/desktop/preload.js` | health normalizer、Office COM 非 Windows 早退、`capability:getAll` IPC | `export:healthCheck`/`office:healthCheck` 保留旧字段并新增 `platform/engine/capability/capabilities/errorCode/message`；macOS Office COM 返回 `PLATFORM_UNSUPPORTED`，不进入 PowerShell；preload 暴露 `getCapabilities` |
| MAC-04：Mac 导出预检 UI 文案和交互 | 已完成，PR #6 已合并 | `code/desktop/renderer/index.html`、`code/desktop/renderer/renderer.js` | 导出引擎文案、LibreOffice/Office 预检弹窗、诊断日志 | macOS Office COM 弹窗显示不可用且禁用继续按钮；LibreOffice 缺失弹窗显示 Homebrew / `LIBREOFFICE_PATH` 动作；真实 Electron DOM/IPC 手工烟测通过并生成 `/tmp/scene-image-tool-mac-office-modal.png`、`/tmp/scene-image-tool-mac-lo-modal.png` |
| MAC-05：Mac LibreOffice 导出 smoke 和脱敏 fixture | 已完成，PR #7 已合并 | `code/desktop/scripts/libreoffice-export-fixture-smoke.js`、`code/desktop/test-fixtures/export-basic/`、`code/desktop/package.json` | 生成式 DOCX/PPTX fixture、LibreOffice PDF/PNG smoke、JSON report | 真实 LibreOffice smoke 2/2 通过；`--runtime-mode embedded` 在 macOS 返回 SKIP + `PLATFORM_UNSUPPORTED`，不误报 pass；输出进入 ignored `code/desktop/_test_output/` |
| MAC-06：macOS unsigned app bundle | 已完成，PR #8 已合并 | `code/desktop/package.json`、`code/desktop/assets/app-icon.icns` | `dist:mac:dir`、macOS `dir` target、`.icns`、平台化 `extraResources` | `dist:mac:dir` 成功生成 `dist/mac-arm64/流量蜂虚拟笔记工具.app`；app 10 秒启动 smoke 通过；包内无 Windows LibreOffice runtime / redist / PowerShell scripts；`dist/` ignored |
| MAC-07：GitHub Actions 基础 CI matrix | 已完成，PR #9 已合并 | `.github/workflows/desktop-ci.yml` | Windows/macOS matrix、Node 22、npm cache、`npm ci`、两个 puzzle smoke、`git diff --check` | PR #9 Actions 已通过 Windows/macOS 两个基础 job；workflow 不调用 LibreOffice、字体探针或打包脚本 |
| MAC-08：Mac PDF / 拼图 / 图片能力验收矩阵 | 已完成，PR #10 已合并 | `code/desktop/scripts/render-fixture-smoke.js`、`docs/current/macos-render-qa-matrix.md` | Skia/Sharp/PDFium render smoke、Compose DOM smoke 记录、QA matrix | `render:fixture:smoke` 3/3 通过；Compose DOM smoke 生成 1600x1000 PNG；两个 puzzle smoke 通过；PR #10 Desktop CI 通过；产物进入 ignored `_test_output/` |
| MAC-09：平台能力设置页 / 诊断区 | 已完成，PR #11 已合并 | `code/desktop/main.js`、`code/desktop/platform/index.js`、`code/desktop/renderer/index.html`、`code/desktop/renderer/renderer.js`、`code/desktop/renderer/styles.css` | `capability:getAll` 汇总扩展、设置页平台能力面板、刷新/加载/unsupported 状态 | macOS Electron DOM 验收通过：设置页显示 LibreOffice、Office COM、PDF render、font、packaging 5 项；LibreOffice `system_app` 可用，Office COM `PLATFORM_UNSUPPORTED`；无横向溢出、无重叠、无 Windows 修复词；PR #11 Desktop CI 通过：Windows 53s、macOS 32s |
| MAC-10：主进程 settings service 最小拆分 | 已完成，PR #12 已合并 | `code/desktop/services/settings-service.js`、`code/desktop/main.js` | 设置读写 helper 和 settings IPC registration 从主进程拆入 service | `main.js` 从 9180 行降至 9105 行；`settings:getAll` / `settings:set` IPC 名称和返回结构不变；临时 userData service smoke、Electron 启动 smoke、两个 puzzle smoke 通过；PR #12 Desktop CI 通过：Windows 47s、macOS 24s |
| MAC-11：资源 artifact / provisioning | 已完成，PR #13 已合并 | `code/desktop/resources/runtime-manifest.json`、`code/desktop/scripts/provision-runtime-artifacts.js`、`code/desktop/package.json` | runtime / fonts manifest、本地 artifact root dry-run、sha256 校验和资源策略文档 | public 仓库仍只跟踪 manifest / script / README / checksum，不提交字体或 runtime 二进制；macOS `resources:check` 命中系统 LibreOffice，缺字体为 warning；sha256 mismatch 用例会阻断；PR #13 Desktop CI 通过：Windows 47s、macOS 39s |
| MAC-12：macOS 签名、公证和发布准备 | 准备完成，PR #14 已合并；真实签名待 Apple 凭据 | `code/desktop/scripts/check-macos-signing-env.js`、`code/desktop/scripts/build-macos-release.js`、`.github/workflows/macos-release.yml` | signing preflight、signed dmg/zip 构建入口、entitlements、release workflow、runbook 和 release notes 模板 | 本机确认 0 个有效 signing identities，且无 `CSC_*` / `APPLE_*` env；缺凭据时 `signing:mac:check` 和 `dist:mac` 均阻断；`dist:mac:dir` unsigned fallback 仍通过；PR #14 Desktop CI 通过：Windows 1m14s、macOS 34s |
| MAC-13：IPC shell 边界和长任务取消 hardening | 已完成，本地验证通过，PR 待提交 | `code/desktop/services/shell-service.js`、`code/desktop/services/request-control.js`、`code/desktop/main.js`、`code/desktop/preload.js`、renderer 打开目录调用点、`code/desktop/package.json` | shell IPC allowlist、已登记目录打开、Feishu/XHS 当前 fetch abort、progress listener unsubscribe、services 打包白名单 | `openExternal` 默认只允许 `https:`；`openPath` 拒绝 URL/相对路径/未登记目录/文件路径；`feishu:cancel` 和 `xhs:cancel` 会 abort 当前请求；request-control body 阶段 timeout/cancel smoke 通过；Electron dev 启动、两个 puzzle smoke、`dist:mac:dir` 和 bundle 启动通过 |
