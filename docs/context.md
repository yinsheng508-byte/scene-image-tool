# CONTEXT

> 长期背景、术语和重大决策。只追加，不随单个任务清空。

## 核心概念

| 术语 | 含义 |
|---|---|
| 流量蜂虚拟笔记工具 | 当前 Electron 桌面应用产品名 |
| 文档一键导出 | 将 Word、PPT、PDF 批量导出为图片 |
| 场景化图片排版 | 上传底图和叠图，通过角点定位合成场景图 |
| 百变拼图排版 | 模板化图片坑位、文字、装饰图和批量生成工具 |
| 飞书一键上传 | 将本地图片上传到飞书多维表格附件字段 |
| 按笔记上传 | 按文件夹/笔记结构扫描图片并顺序写入飞书记录 |
| 小红书商品下载 | 从小红书商品链接提取并下载图片 |
| 授权管理 | 授权密钥、微信验证码登录、版本更新检查 |

## 长期决策

- 2026-08-26：采用 `yinsheng508-byte/ai-project-template` 的治理结构，但不直接覆盖业务代码。
- 2026-08-26：迁移分三步推进：先治理文档入口，再迁移代码目录，最后治理大文件和历史。
- 2026-08-26：结构迁移阶段不做业务逻辑重构，避免把目录变化和功能变化混在一个风险面。

## 当前结构决策

- 2026-08-26：应用运行入口已从 `desktop/` 迁移为 `code/desktop/`。
- 2026-08-26：后续开发、验证和打包命令统一使用 `npm --prefix code/desktop ...`。
- `docs/` 只保留协作过程、架构记忆、任务、验收和历史归档。
- 可运行、可打包、可交付的应用主体和交付说明进入 `code/`。
